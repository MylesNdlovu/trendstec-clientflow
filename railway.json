{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd mcp-servers/mt5-playwright && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd mcp-servers/mt5-playwright && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
